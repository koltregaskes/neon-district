{
  "name": "neon-district",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "preview": "vite preview",
    "capture:review": "npm run build && node scripts/capture-review.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "~5.9.3",
    "vite": "^8.0.8"
  }
}
